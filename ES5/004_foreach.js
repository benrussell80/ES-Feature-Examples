// forEach method of Array
// Use for each when you don't want to return something
let myAnnouncements = ['Great job!', 'Wow!', 'So cool!'];

let returnValue = myAnnouncements.forEach(
  (element) => console.log(element)
);

console.log(returnValue);
